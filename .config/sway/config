# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:"JetBrainsMono Nerd Font" 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +3% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -3% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
# LDA 2020-03-22 reinstalling Pulseaudio so commenting out these ALSA equivalents.
# bindsym XF86AudioRaiseVolume exec --no-startup-id "amixer -q sset Master,0 5+ unmute"
# bindsym XF86AudioLowerVolume exec --no-startup-id "amixer -q sset Master,0 5- unmute"
# bindsym XF86AudioMute exec --no-startup-id "amixer -q sset Master,0 toggle && $refresh_i3status"
#
# 2020-03-22 LDA: choosing HDMI profile on sound card, instead of analog (jack) output:
# pactl set-card-profile 0 output:hdmi-stereo
# See "pactl list cards" for the list of cards, and the "Profils" section in the output for the available profiles.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec alacritty

# kill focused window
bindsym $mod+Shift+q kill

# start menu (a program launcher)
# bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# LDA: original command with no specific font.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop --dmenu="dmenu -i -fn 'Fira Code:condensed:pixelsize=30'" --entry-type=desktop
# See: https://wiki.archlinux.org/title/Rofi → Contributed themes
# bindsym $mod+d exec rofi -show drun
bindsym $mod+d exec --no-startup-id wofi --gtk-dark --show drun -run-shell-command '{terminal} -e bash -ic "{cmd} && read"'

# change focus
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+h focus left
bindsym $mod+l focus right
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
bindsym $mod+Shift+h move left

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
# LDA removed because it's focus left now!
# bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
# bindsym $mod+Shift+space floating toggle
# LDA Fri, 29 Apr 2022 18:21:41 +0200
# Trying another binding since setxkbmap is using $mod+space and taking priority.
# Mod1 is Alt
bindsym Control+Mod1+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
# LDA use wev (paru -S wev) to get the names of these symbols.
# Some are duplicated because of the fr-dev and fr symbols sets.
bindsym --to-code {
	$mod+ampersand workspace number $ws1
	$mod+eacute workspace number $ws2
	$mod+asciitilde workspace number $ws2
	$mod+quotedbl workspace number $ws3
	$mod+apostrophe workspace number $ws4
	$mod+parenleft workspace number $ws5
	$mod+minus workspace number $ws6
	$mod+egrave workspace number $ws7
	$mod+backslash workspace number $ws7
	$mod+underscore workspace number $ws8
	$mod+ccedilla workspace number $ws9
	$mod+grave workspace number $ws9
	$mod+agrave workspace number $ws10
	$mod+at workspace number $ws10
}

# move focused container to workspace
bindsym $mod+Shift+ampersand move container to workspace number $ws1
bindsym $mod+Shift+eacute move container to workspace number $ws2
bindsym $mod+Shift+asciitilde move container to workspace number $ws2
bindsym $mod+Shift+quotedbl move container to workspace number $ws3
bindsym $mod+Shift+apostrophe move container to workspace number $ws4
bindsym $mod+Shift+parenleft move container to workspace number $ws5
bindsym $mod+Shift+minus move container to workspace number $ws6
bindsym $mod+Shift+egrave move container to workspace number $ws7
bindsym $mod+Shift+backslash move container to workspace number $ws7
bindsym $mod+Shift+underscore move container to workspace number $ws8
bindsym $mod+Shift+ccedilla move container to workspace number $ws9
bindsym $mod+Shift+grave move container to workspace number $ws9
bindsym $mod+Shift+agrave move container to workspace number $ws10
bindsym $mod+Shift+at move container to workspace number $ws10

# Reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# LDA changing default line sinces it requires to have a mouse...
bindsym $mod+Shift+e exec "swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'swaymsg exit'"
# bindsym $mod+Shift+e exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

bar {
# LDA these options go for the default command "swaybar".
# For waybar see ~/.config/waybar/config and the CSS styling.
#	height 6
#	font Cantarell Thin 6
	swaybar_command waybar
}

default_border none

# To get identifiers on Sway, run: swaymsg -t get_tree

for_window [app_id="^Alacritty$"] border none
for_window [class="^Inkstitch$"] floating enable
for_window [title="^Variables$"] floating enable, border normal
for_window [class="^inkstitch-gui$"] floating enable
for_window [title="^Connectez-vous à votre compte PayPal - Mozilla Firefox$"] floating enable
# vncviewer in floating windows to keep the initial geometry
for_window [class="^Vncviewer$"] floating enable

assign [class="^Slack"] 1
for_window [app_id="^firefox$"] move --no-auto-back-and-forth to workspace 3
for_window [class="^Min$"] move --no-auto-back-and-forth to workspace 3
for_window [app_id="^mullvadbrowser$"] move --no-auto-back-and-forth to workspace 3

for_window [class="^Zathura$"] move --no-auto-back-and-forth to workspace 8
for_window [app_id="^org.pwmt.zathura$"] move --no-auto-back-and-forth to workspace 8
for_window [title="Seamly2D"] move --no-auto-back-and-forth to workspace 8

# Run "xev" and then press a key to retrieve its keycode.
# Run "xmodmap -pke" and see if the keycode you get has an alias, such as "Print" for keycode 107.
#
# LDA Tue, 09 May 2023 15:12:39 +0200 there is no simple "Print" key on the Starlite laptop, the
# code is fired up on Control+F12, but thanks to --to-code, it works with F12 alone.
# → Not sure about that. Maybe it changes nothing.
bindsym --to-code {
	# Screenshot and edit the whole, focused display.
	Print exec grim -o $(swaymsg -t get_outputs | \
		jq -r '.[] | select(.focused) | .name') - | \
		satty -f - -o "/home/luc/Captures d'écran/satty_%F_%T.png"

	# Screenshot and edit a custom selection.
	Shift+Print exec grim -g "$(slurp)" - | \
		satty -f - -o "/home/luc/Captures d'écran/satty_%F_%T.png"

	# Screenshot and edit a selected window.
	# DEV
	# Control+Shift+s exec grim -g "$(slurp)" - | swappy -f -
	# FIXME This does not work, why?
	# Alt+Shift+Print
	Control+Shift+Print exec grim -g "$(swaymsg -t get_tree | jq -r '.. | select(.pid? and .visible?) | .rect | "\(.x),\(.y) \(.width)x\(.height)"' | slurp -r)" - | \
		satty -f - -o "/home/luc/Captures d'écran/satty_%F_%T.png"

	# Grab a screenshot from the focused window under Sway, using swaymsg and jq:
	# Mod1+Shift+s exec grim -g "$(swaymsg -t get_tree | jq -j '.. | select(.type?) | select(.focused).rect | "\(.x),\(.y) \(.width)x\(.height)"')"
}

# TODO to select a window. See https://github.com/emersion/slurp
# bindsym Control+Shift+s exec --no-startup-id swaymsg -t get_tree | jq -r '.. | select(.pid? and .visible?) | .rect | "\(.x),\(.y) \(.width)x\(.height)"' | slurp

bindsym XF86MonBrightnessUp exec --no-startup-id "xbacklight -inc 15 -fps 30"
bindsym XF86MonBrightnessDown exec --no-startup-id "xbacklight -dec 15 -fps 30"

workspace_auto_back_and_forth yes

for_window [class="^scrcpy$"] floating disable
for_window [class="^Gvncviewer$"] floating enable

# Inkscape & Inkscape modules
for_window [app_id="^inkstitch$"] floating enable
# LDA this does not work…
for_window [app_id="^inkstitch$" title="^Embroidery Params$"] move absolute 0 0

gaps inner 15
# Gaps only with two or more windows
smart_gaps on

#
# Inputs Configuration
#
# See https://man.archlinux.org/man/sway-input.5
# See https://man.archlinux.org/man/xkeyboard-config.7
# See https://wiki.archlinux.org/title/Sway#Keymap
input * {
    xkb_layout "fr-dev,fr,us"
	# It was this up to a system upgrade on Sat, 17 Jun 2023 08:00. Now "ar" is not found in
	# symbols. To be fixed.
	# xkb_layout "fr-dev,fr,ar"
	xkb_variant "basic,optimot_iso_18,basic"

    # xkb_variant "colemak,,typewriter"
    xkb_options "grp:win_space_toggle"
	# initially enable numlock on all keyboards
	xkb_numlock enabled
}

# Apple Magic Keyboard (Dreem Legacy)
# swaymsg -t get_inputs
input 1452:620:Apple_Inc._Magic_Keyboard_with_Numeric_Keypad {
	xkb_layout "fr-dev,fr,us"
    xkb_options "grp:win_space_toggle,altwin:swap_lalt_lwin,altwin:swap_alt_win,lv3:rwin_switch"
}

# Lock
# TODO suspend dpms a few seconds after we lock the screen.
bindsym Control+Mod1+l exec --no-startup-id swaylock \
					--daemonize \
					--show-failed-attempts \
					--show-keyboard-layout \
					--color 000000 \
					--image ~/Photos/Wallpapers/forrest.png \
					--scaling fit

# Notifications manager
exec dunst

# See https://wiki.archlinux.org/title/Sway#Configuration
include /etc/sway/config.d/*
# Custom per-host configuration.
include ~/.config/sway/`hostname`.conf
